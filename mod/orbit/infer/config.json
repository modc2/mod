{
    "name": "infer",
    "version": "0.1.0",
    "description": "One aggregator over every model-inference router that settles in crypto and asks for no documents \u2014 an OpenRouter over the routers. Six catalogs (NanoGPT, Venice, PPQ, Chutes, io.net, OpenRouter) are fetched at once and merged into one market: ~1,350 offerings of ~1,120 distinct models, text and image and audio and video and embeddings, one row per model with every router that serves it ranked cheapest first. The hard part is not the fan-out, it is that no two routers publish prices in the same unit \u2014 and two of them use the SAME KEY NAME for a per-token price and a per-million-token one, so a normalizer that sniffs field names mis-ranks a router by a factor of a million and silently never routes to it. Units are therefore declared by each adapter, never inferred, and everything downstream is USD per million tokens. What that buys is measurable: 217 models are served by more than one no-KYC router and the spread between dearest and cheapest reaches 5.6x for identical weights. Calls route to the cheapest funded router and fail over to the next when one refuses; each answer carries a receipt naming who served it, what it actually cost from the usage they reported, and what the catalog estimated, so a router billing off its own published prices shows up as drift in the ledger. Settlement is crypto and runs in the background, disarmed by default: the watcher prices top-ups and files them as proposals, and arming it to move funds unattended takes confirm=true plus a rail and a 24h cap. Private keys never enter this module \u2014 transfers are handed to the eth, solana or near modules, which hold them. Still carries both older halves: the temperature-0 receipts board, and the ONNX optimizer for when the cheapest router is no router at all.",
    "icon": "\u26a1",
    "anchor": "mod.py",
    "protocol": "mcp/2025-06-18",
    "port": 50820,
    "app_port": 3820,
    "gateway_port": 50820,
    "mcp_port": 50820,
    "base_path": "/infer",
    "route": true,
    "urls": {
        "api": "http://localhost:50820",
        "app": "http://localhost:3820/infer",
        "mcp": "http://localhost:50820/mcp",
        "onnxruntime_web": "https://cdn.jsdelivr.net/npm/onnxruntime-web/dist/"
    },
    "binary": {
        "format": "ONNX",
        "local": "onnxruntime \u2014 CPU, plus any execution provider this build ships",
        "browser": "onnxruntime-web \u2014 wasm + SIMD, threads when the page is cross-origin isolated, WebGPU where available",
        "why": "one file, two runtimes, no second conversion \u2014 the bytes benchmarked here are the bytes the browser runs",
        "verified": "a fused model was loaded from /blob and run in onnxruntime-web on 2026-08-29: contrib ops (FusedConv, BiasGelu) ran, com.microsoft.nchwc.Conv did not"
    },
    "passes": {
        "slim": "strip doc strings, training info and unused initializers (lossless)",
        "shapes": "shape inference, so every intermediate tensor is annotated (lossless)",
        "basic": "constant folding, dead node and redundant cast/identity elimination (lossless)",
        "extended": "basic + operator fusion: Conv+BN, MatMul+Add, GELU, attention (lossless; emits onnxruntime contrib ops, which the browser wasm backend does register)",
        "all": "extended + layout transforms for the CPU that ran them (lossless, and genuinely unloadable in a browser: com.microsoft.nchwc.*)",
        "fp16": "half-precision weights with fp32 inputs and outputs kept (lossy, ~2x smaller)",
        "int8": "dynamic int8 weight quantization, no calibration data needed (lossy, ~4x smaller)",
        "uint8": "the same in uint8 \u2014 some kernels only have a fast path for one of the two"
    },
    "default_passes": [
        "slim",
        "extended"
    ],
    "state": "~/.mod/infer/ (models/<sha256>.onnx, registry.json, browser.json, router-catalog.json, router-ledger.jsonl, router-keys.json 0600, settle.json, settle-addresses.json 0600, settle-proposals.jsonl)",
    "endpoints": {
        "info": "GET /",
        "health": "GET /health",
        "mcp": "POST /mcp",
        "app": "GET /infer \u2014 Next.js console on app_port 3820; this path on the API port answers with the route map",
        "models": "GET /models | POST /models {data|path|url, name?} | DELETE /models/:id",
        "blob": "GET /blob/:id \u2014 the raw .onnx bytes, what the browser fetches",
        "inspect": "GET /inspect?model=",
        "plan": "GET /plan?model=&target=local|web",
        "passes": "GET /passes",
        "optimize": "POST /optimize {model, passes?, batch?, runs?, samples?, tol?}",
        "bench": "GET|POST /bench?model=&runs=&batch=&threads=&provider=",
        "parity": "GET|POST /parity?a=&b=&samples=&tol=",
        "portable": "GET /portable?model=",
        "compare": "POST /compare {model, passes?} \u2014 every pass side by side",
        "export": "POST /export {source, shape?, weights?} \u2014 torch \u2192 ONNX",
        "examples": "POST /examples \u2014 plant an MLP, a CNN and a transformer block",
        "report": "POST /report {model, ms, ...} \u2014 record what a browser measured",
        "reports": "GET /reports?model= \u2014 browser numbers next to local ones",
        "tools": "GET /tools",
        "router": "GET /router \u2014 every router and the terms it trades on",
        "router_models": "GET /router/models?q=&input=&output=&coin=&kyc=&max_usd=&min_context=&multimodal=&free=&sort=&limit= \u2014 one row per model, every router that serves it, cheapest first",
        "router_offerings": "GET /router/offerings?... \u2014 one row per (router, model)",
        "router_modalities": "GET /router/modalities \u2014 what the registry can do, counted",
        "router_refresh": "GET /router/refresh \u2014 re-fetch every catalog now",
        "router_plan": "GET /router/plan?model=&require=&kyc= \u2014 the ranking, spending nothing",
        "router_chat": "POST /router/chat {model, prompt|messages, require?, confirm?} \u2014 route, call, fail over",
        "router_key": "POST /router/key {provider, key}",
        "router_spend": "GET /router/spend \u2014 per-router cost, latency and billing drift",
        "router_ledger": "GET /router/ledger?provider=&limit=",
        "settle": "GET /settle \u2014 watcher state, arming, caps, 24h spend",
        "settle_balances": "GET /settle/balances \u2014 what each funded router has left",
        "settle_rails": "GET /settle/rails \u2014 which chain modules are reachable",
        "settle_policy": "GET|POST /settle/policy {armed, rail, coin, floor_usd, topup_usd, daily_cap_usd, confirm}",
        "settle_sweep": "POST /settle/sweep \u2014 one pass: read balances, propose or execute",
        "settle_proposals": "GET /settle/proposals?state=",
        "settle_pay": "POST /settle/pay {proposal|provider, usd, address?, rail, coin, confirm}",
        "settle_address": "POST /settle/address {provider, address, coin}",
        "settle_start": "POST /settle/start \u2014 run the sweep on a timer",
        "settle_stop": "POST /settle/stop"
    },
    "backend": "python: http.server + onnx + onnxruntime (torch only for export), in src/; console: Next.js 14 in src/app, basePath /infer, /_api proxied to the python port",
    "requires": {
        "onnx": "graph reading and rewriting",
        "onnxruntime": "execution, graph optimization, quantization, fp16",
        "numpy": "input generation and output comparison",
        "torch": "optional \u2014 only `export` and `examples` need it"
    },
    "tools": [
        "infer_add",
        "infer_bench",
        "infer_board",
        "infer_canon",
        "infer_compare",
        "infer_delete",
        "infer_diff",
        "infer_examples",
        "infer_export",
        "infer_health",
        "infer_import",
        "infer_inspect",
        "infer_leaderboard",
        "infer_models",
        "infer_optimize",
        "infer_parity",
        "infer_passes",
        "infer_plan",
        "infer_portable",
        "infer_post",
        "infer_providers",
        "infer_question",
        "infer_receipt",
        "infer_replicate",
        "infer_router_chat",
        "infer_router_key",
        "infer_router_modalities",
        "infer_router_models",
        "infer_router_plan",
        "infer_router_spend",
        "infer_routers",
        "infer_run",
        "infer_set_key",
        "infer_settle",
        "infer_settle_balances",
        "infer_settle_pay",
        "infer_settle_policy",
        "infer_settle_sweep",
        "infer_status",
        "infer_verify"
    ],
    "fns": [
        "add",
        "balances",
        "bench",
        "chat",
        "compare",
        "delete",
        "examples",
        "export",
        "forward",
        "health",
        "info",
        "inspect",
        "kill",
        "ls",
        "market",
        "mcp_call",
        "mcp_config",
        "modalities",
        "models",
        "opt",
        "optimize",
        "parity",
        "passes",
        "pay",
        "plan",
        "plan_route",
        "portable",
        "readme",
        "rm",
        "route",
        "router_models",
        "routers",
        "serve",
        "settle",
        "spend",
        "sweep",
        "test",
        "tools",
        "what"
    ],
    "env": {
        "PORT": "port for API, console and MCP (default 50820)",
        "INFER_DIR": "where models are stored (default ~/.mod/infer)",
        "INFER_RUNS": "default timed iterations per benchmark (30)",
        "INFER_WARMUP": "default untimed iterations first (5)",
        "INFER_MAX_BYTES": "largest model accepted (default 512 MiB)",
        "INFER_SPEND_USD": "per-call spend guard for the router (default 0.50)",
        "INFER_CATALOG_TTL": "seconds a merged catalog stays fresh (default 900)"
    },
    "auth": {
        "reads": "open",
        "writes": "open on loopback for models and the catalog; settlement is gated by its own arming and confirm flags rather than by a token, because the thing being protected is funds leaving a wallet this module does not hold"
    },
    "schema": "QmPces4d7dmEYfd3hi4oCV493MriszHfvL1iPCNisY78bf",
    "halves": {
        "router": "every crypto-settled, no-KYC inference router as one multimodal catalog, routed by price and paid in the background",
        "proofs": "post what a model said at temperature 0, hash it, sign it, publish it, and let anyone re-run the question",
        "optimize": "ONNX in, a smaller and faster ONNX out, running unchanged here and in a browser tab"
    },
    "routers": {
        "nanogpt": "kyc none, no account at all; XMR/BTC/ETH/USDC/LTC/SOL/XNO; 601 models",
        "venice": "kyc none; VVV/USDC/ETH/BTC, quotes a DIEM column; 362 models incl. image, TTS, embeddings",
        "ppq": "kyc none, key bought with crypto; BTC/ETH/USDC/SOL; 369 models",
        "chutes": "kyc none, Bittensor-native; TAO/USDC; TEE-attested chutes",
        "ionet": "kyc account, Solana-native; IO/SOL/USDC; publishes measured latency",
        "openrouter": "kyc account, no documents; USDC on Base; 447 models, the yardstick"
    },
    "price_units": {
        "canonical": "USD per million tokens, everywhere downstream",
        "per_token": "openrouter (strings), io.net (floats)",
        "per_million_tokens": "nanogpt (declares its own unit), ppq, venice, chutes",
        "trap": "openrouter and chutes both publish `pricing.prompt`; openrouter means per-token and chutes means per-million. Units are declared by the adapter, never sniffed from the key name."
    },
    "settlement": {
        "default": "disarmed \u2014 the watcher files proposals, nothing moves",
        "arming": "POST /settle/policy {armed:true, confirm:true, rail, daily_cap_usd}",
        "rails": "eth :50730, solana :50710, near :50910 \u2014 each holds its own key",
        "guards": "per-top-up size cap (4x configured), rolling 24h ceiling, deposit address must be on file",
        "spend_guard": "INFER_SPEND_USD (default 0.50); an unpriced model counts as OVER the limit"
    }
}