networks:
  default:
    external: true
    name: modnet
services:
  ipfs:
    container_name: ipfs
    deploy: {}
    entrypoint: bash -c "m serve port=50054 key=ipfs remote=0 mod=ipfs"
    image: mod:latest
    ports:
    - 50054:50054
    restart: unless-stopped
    shm_size: 100g
    volumes:
    - ~/mod:/root/mod
    - ~/.mod:/root/.mod
    - ~/mod/mod/modules/ipfs:/root/mod/mod/modules/ipfs
    working_dir: /root/mod/mod/modules/ipfs
version: '3.8'
